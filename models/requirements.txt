# ============================================
# edge_infer_cloud - models 统一依赖
# 涵盖: water_inspection, park_monitoring, construction_safety, cloud_infer
# PyTorch 需单独安装: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# ============================================

# ---------- 深度学习核心 ----------
# PyTorch 不在此文件中安装，请根据 CUDA 版本单独安装：
#   CUDA 12.8: pip install torch==2.10.0+cu128 torchvision==0.25.0+cu128 --index-url https://download.pytorch.org/whl/cu128
#   CPU only:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# YOLO 系列
ultralytics>=8.0.180

# ---------- C-RADIOv4 开放词汇分割 ----------
safetensors>=0.4.0
transformers>=4.36.0
timm>=0.9.0
einops>=0.7.0

# ---------- 数值计算与图像处理 ----------
numpy>=1.24.0
opencv-python>=4.8.0
pillow>=9.5.0

# ---------- 数据增强 ----------
albumentations>=1.3.0

# ---------- 可视化 ----------
matplotlib>=3.7.0
seaborn>=0.12.0

# ---------- 通信与服务 ----------
paho-mqtt>=1.6.0
flask>=3.0.0

# ---------- 通用工具 ----------
pyyaml>=6.0
tqdm>=4.65.0
pycocotools>=2.0.6

# ---------- MLflow 模型管理（训练场景） ----------
# MLflow — 已禁用。ultralytics 自动注册 mlflow callback，无 MLflow server 时导致训练崩溃
# 如需启用，请确保 MLFLOW_TRACKING_URI 已配置
# mlflow>=2.9.0

# ---------- ONNX 导出（边缘部署场景） ----------
onnx>=1.15.0
onnxruntime>=1.16.0
